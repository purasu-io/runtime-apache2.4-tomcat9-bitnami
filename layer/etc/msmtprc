# https://wiki.debian.org/msmtp
account default
port 587
tls on
tls_starttls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
tls_certcheck on
logfile /dev/stdout
auth on
# host SMTP_HOSTNAME
# user SMTP_USERNAME
# password "SMTP_PASSWORD"
